node_modules/
dist/
coverage/
.warden/ledger.db
.env
*.log
docs/internal/
docs/qa/
# Local agent planning and handoff artifacts. Never commit these files.
docs/superpowers/

# Claude Code's convention for personal, untracked hook config — this is
# where our own docs tell users to put a literal WARDEN_AUTH_TOKEN value
# (see README's Quick Start step 4 / docs/MANUAL.md), so keep it out of this
# repo's own history too.
.claude/settings.local.json
