{
  "name": "warden",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/hook-server",
    "packages/mcp-gateway",
    "packages/cli"
  ],
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/bun": "^1.3.13",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  }
}
